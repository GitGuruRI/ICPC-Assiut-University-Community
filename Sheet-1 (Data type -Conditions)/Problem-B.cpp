#include <bits/stdc++.h>
using namespace std;

int main() {
    // declare basic data types variable
    int num1;
    long long num2;
    char letter;
    float fnum;
    double dnum;

    // get user input
    cin >> num1 >> num2 >> letter >> fnum >> dnum;

    // print data type variable value
    cout << num1 << endl << num2 << endl << letter << endl << fnum << endl << dnum;
    
    return 0;
}