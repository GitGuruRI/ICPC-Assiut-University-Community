#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
    ll a, b, c, d;
    cin >> a >> b >> c >> d;

    // difference
    cout << "Difference = " << (a * b) - (c * d);

    return 0;
}