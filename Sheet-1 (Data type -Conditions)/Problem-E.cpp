#include <bits/stdc++.h>
using namespace std;

int main() {
    double r, pi = 3.141592653;
    cin >> r;

    // circle area
    cout << fixed << setprecision(9);
    cout << pi * r * r;

    return 0;
}